//import statements, if needed

//Test suite: sanity check
describe('Sanity check in main.test.js: Mocha', () => {
    //Test Spec (unit test)
    it('should run our tests using npm', () => {
        chai.expect(true).to.be.ok;
    });
});

