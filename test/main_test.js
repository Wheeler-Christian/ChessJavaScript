var expect = require('chai').expect;

//Test suite
//sanity check
describe('Sanity check in main: Mocha', () => {
    //Test Spec (unit test)
    it('should run our tests using npm', () => {
        expect(true).to.be.ok;
    });
});